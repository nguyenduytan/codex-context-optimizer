# Policies

Automatic effort is capped at medium. `CHAT` uses minimal, `MICRO` low, normal `CODE` low and `COMPLEX` medium. Explicit `--effort` is the user's opt-in override. Agents are off, retries are zero, maps and tool output are bounded, and execution guidance stops at acceptance. `--passthrough` disables compiler/runtime optimization but keeps process safety.
