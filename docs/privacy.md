# Privacy

ctxc adds no network service, API key or telemetry, and never calls a second model. Codex provider traffic remains governed by Codex. Bounded local records contain compiler/runtime metadata and observed usage, not prompt/response bodies. Do not commit `.ctxc` private state.
